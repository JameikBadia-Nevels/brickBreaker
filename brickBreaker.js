let brks = document.querySelector("#bricks")

var brickArr = [], numBrickCol = 8, numBrickRow = 4,  

//creating the bricks
const brickCreate = () => {
    for(let i = 0; i < numBrickCol; i++){
        for(let k = 0; k < numBrickRow; k++){
            // brickArr[brickIndex] = new 
        }
    }
}